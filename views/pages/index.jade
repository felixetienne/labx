extends layout

block header
    include ../included/header.jade

block content
    .container-narrow
        include ../included/topnav.jade
        h1(class="muted") #{content.title}
        hr
        div(class="jumbotron")
          p(class="lead") #{content.text}
