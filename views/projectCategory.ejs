<div class="mod-articles mod-articles--projects">
  <% include included/standardContent.ejs %>
  <div class="marts--list">
    <%
    var iTotal = projects.length;
    for(var i=0; i<iTotal; i++){
      var project = projects[i]; %>
      <article class="marts--article">
        <h1><%- project.title %></h1>
        <p>
          <%- project.description %>
          <a class="marts--more" href="<%- project.url %>" title="Lire plus">Lire plus...</a>
        </p>
        <%
        for(var j=0; j< project.images.length; j++){
          var image = project.images[j]; %>
          <a href="<%- project.url %>" title="Lire plus">
            <img src="<%- image.path %>" alt="<%- image.title %>" />
          </a>
        <%}%>
      </article>
    <%}%>
  </div>
</div>
