<% if(meta.hasError()) {%>
  <h1>Error</h1>
  <% console.log(meta.getErrors()); meta.getErrors().forEach(function(error){  %>
    <p><%- error.getCode() %>. <%- error.getMessage()%></p>
  <%})%>
  <a href="/">Retourner à la page d'accueil.</a>
<%}%>
<% if(!meta.hasError()) {%>
  <h1><%- page.title %></h1>
  <p><%- page.description %></p>
<%}%>
