<script type="text/javascript">
  window.currentContext = (function(context){
    <% if(!meta.hasError){%>

      context.version = <%- website.version %>;

      context.projectName = <%- website.title %>;

    <%}%>
    return context;
  })(window.currentContext || {});
</script>
